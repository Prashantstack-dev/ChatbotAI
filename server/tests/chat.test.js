//Input validation
test('returns 400 when message is missing')
test('return 400 when message is empty')
test('return 400 when message exceeds 100 chars')
test('return 400 when message is not a string')

//RAG Pipeline(mocked)
test('')